#include "ncurses_display.h"
#include "system.h"
#include <iostream>
#include <iomanip>

int main() {
  System system;
  //auto processes = system.Processes(); 
  //for(int i=0; i<10; i++){
  //    for(auto &process: processes){
  //        std::cout << std::setfill(' ') << std::setw(4) << process->Pid() << " "; 
  //        //std::cout << std::setfill(' ') << std::setw(6) << "Cpu: " << process->CpuUtilization() << std::endl; 
  //        //std::cout << std::setfill(' ') << std::setw(4) << process->Pid() << " "; 
  //        process->CpuUtilization(); 
  //        std::cout << std::setfill(' ') << std::setw(6) << "Cpu: " << process->cpu_percentage_ << std::endl; 
  //    }
  //    std::cout << "-------------------" << std::endl; 
  //    processes = system.Processes();
  //    //for(auto &process: processes){
  //    //    std::cout << std::setfill(' ') << std::setw(4) << process->Pid() << " "; 
  //    //    //process->CpuUtilization(); 
  //    //    std::cout << std::setfill(' ') << std::setw(6) << "Cpu: " << process->cpu_percentage_ << std::endl; 
  //    //}
  //}
  //NCursesDisplay::Display(system);
}
